name: Build and Send APK

on:
  push:
    branches: [dev]

jobs:
  build_and_upload_apk:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'  # Specific Node.js version for consistency

      - name: Install Dependencies
        run: npm install nodemailer

      - name: Build APK (Debug) # or assembleRelease for production
        id: build_apk
        run: |
          # Make sure gradlew is executable
          chmod +x gradlew

          # Assuming you're using Gradle. Adjust command as needed.
          ./gradlew assembleDebug # or assembleRelease

          # Check if the APK was created
          if [ -f "android/app/build/outputs/apk/debug/app-debug.apk" ]; then
            echo "APK_PATH=android/app/build/outputs/apk/debug/app-debug.apk" >> $GITHUB_ENV
            echo "::set-output name=apk_built::true" # Set output variable
          else
            echo "❌ APK not found! Build failed or path is incorrect."
            exit 1  # Exit the step, indicating failure
          fi
        shell: /usr/bin/bash -e {0}

      - name: Upload APK to Upload.app
        if: steps.build_apk.outputs.apk_built == 'true'
        id: upload_app
        run: |
          # Upload the APK to upload.app
          curl --request POST \
            --url 'https://upload.app/api/upload' \
            --header 'Content-Type: multipart/form-data' \
            --form 'file=@${{ env.APK_PATH }}' > upload_response.json

          # Extract the link from the JSON response
          UPLOAD_LINK=$(jq -r '.url' upload_response.json)
          echo "UPLOAD_LINK=$UPLOAD_LINK" >> $GITHUB_ENV

          echo "✅ APK uploaded to Upload.app! Link: $UPLOAD_LINK"
        shell: /usr/bin/bash -e {0}

  send_email:
    needs: build_and_upload_apk
    if: always() # Ensures email is sent regardless of prior step success/failure
    runs-on: ubuntu-latest
    env:
      BREVO_HOST: "smtp-relay.brevo.com"
      BREVO_PORT: 587
      BREVO_USERNAME: "86daf4001@smtp-brevo.com"
      BREVO_PASSWORD: "9HUFhqYPs4xgNJAc"
      RECIPIENT_EMAIL: "vivek@glorywebs.com"  # For testing
      SENDER_EMAIL: "parthglory2025@gmail.com"  # Verified Sender in Brevo
    steps:
      - name: Send Email with Brevo SMTP
        run: |
          INSTALL_LINK="${{ env.UPLOAD_LINK }}"
          NODE_OPTIONS="--tls-min-v1.1" node <<EOF
          const nodemailer = require('nodemailer');

          async function main() {
            const installLink = process.env.UPLOAD_LINK || "No link available (build or upload failed)"; // Use link, or fallback
            const success = process.env.UPLOAD_LINK ? true : false;

            let transporter = nodemailer.createTransport({
              host: process.env.BREVO_HOST,
              port: process.env.BREVO_PORT,
              secure: false,
              auth: {
                user: process.env.BREVO_USERNAME,
                pass: process.env.BREVO_PASSWORD,
              },
              tls: {
                ciphers: 'SSLv3',
                rejectUnauthorized: false
              }
            });

            let info = await transporter.sendMail({
              from: "GitHub Actions <${process.env.SENDER_EMAIL}>",
              to: process.env.RECIPIENT_EMAIL,
              subject: success ? "Your APK is Ready!" : "APK Build/Upload Failed",
              text: success ? `Download your APK here: ${installLink}` : "APK build or upload failed. Check the workflow logs for details.",
              html: success ? `<p>Your APK is ready! Click below to download:</p><a href="${installLink}">${installLink}</a>` : "<p>APK build or upload failed. Check the workflow logs for details.</p>",
            });

            console.log("✅ Email sent: %s", info.messageId);
          }

          main().catch((error) => {
            console.error("❌ Error sending email:", error);
            process.exit(1);
          });
          EOF